import { defineComponent } from 'vue'

export default defineComponent({
  name: 'an-switch',
  setup () {
    return () => {
      return (
        <div class="an-switch">
          Switch components working;
        </div>
      )
    }
  }
})